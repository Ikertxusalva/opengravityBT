# ── Cloud Backend ──
DATABASE_URL=postgresql+psycopg2://og_user:og_dev_password@localhost:5432/opengravity

# ── Railway (for production) ──
# DATABASE_URL is auto-set by Railway when you add a Postgres plugin

# ── API Keys (for future agents) ──
# HELIUS_API_KEY=
# GROQ_API_KEY=
